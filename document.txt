<p><strong>  CHAT BOT VICKA TURISMO  </strong></p><p>  Sistema Omnicanal con IA — Arquitectura para Producción  </p><table><tr><td><p><strong>WhatsApp + Messenger + Instagram</strong></p></td><td><p><strong>Node.js + Claude API + PostgreSQL</strong></p></td><td><p><strong>Listo para Railway / Vercel / VPS</strong></p></td></tr></table><p><em>Versión 1.0  •  Preparado para Antigravity</em></p><h1><strong>1. Arquitectura del Sistema</strong></h1><p>El sistema adopta una arquitectura de microservicios desacoplados con un único punto de entrada para webhooks. Todos los canales convergen en el mismo backend, que normaliza los mensajes antes de procesarlos con IA.</p><h2><strong>1.1 Capas del sistema</strong></h2><table><tr><td><p><strong>Capa</strong></p></td><td><p><strong>Responsabilidad</strong></p></td></tr><tr><td><p><strong>Canales (Meta APIs)</strong></p></td><td><p>WhatsApp, Messenger, Instagram envían eventos al webhook</p></td></tr><tr><td><p><strong>Webhook Router</strong></p></td><td><p>Valida firma HMAC, identifica canal, parsea payload</p></td></tr><tr><td><p><strong>Message Processor</strong></p></td><td><p>Normaliza mensaje, detecta intención con Claude, enruta</p></td></tr><tr><td><p><strong>Claude AI Engine</strong></p></td><td><p>Genera respuesta contextual usando system prompt + FAQs + historial</p></td></tr><tr><td><p><strong>Response Sender</strong></p></td><td><p>Envía respuesta al canal correcto via API de Meta</p></td></tr><tr><td><p><strong>Data Layer</strong></p></td><td><p>PostgreSQL (persistencia), Redis (sesiones/caché), Config Store</p></td></tr><tr><td><p><strong>Panel Admin</strong></p></td><td><p>UI para configurar bot, FAQs, horarios, ver conversaciones</p></td></tr></table><h2><strong>1.2 Stack tecnológico</strong></h2><ul><li>Backend: Node.js 20 LTS + Express 4</li><li>Base de datos principal: PostgreSQL 15</li><li>Caché y sesiones: Redis 7</li><li>IA: Claude Sonnet via Anthropic SDK</li><li>Deploy: Railway (recomendado) o VPS con Docker</li><li>Integraciones: Meta Graph API v19+</li><li>Autenticación admin: JWT + bcrypt</li></ul><h1><strong>2. Estructura de Carpetas</strong></h1><p><strong>  vicka-turismo-bot/</strong></p><p> </p><p>vicka-turismo-bot/</p><p>├── src/</p><p>│   ├── config/</p><p>│   │   ├── database.js          # Conexión PostgreSQL (pg + pool)</p><p>│   │   ├── redis.js             # Conexión Redis</p><p>│   │   └── index.js             # Variables de entorno centralizadas</p><p>│   │</p><p>│   ├── webhooks/</p><p>│   │   ├── index.js             # Router principal /webhook</p><p>│   │   ├── validator.js         # Verificación HMAC-SHA256 de Meta</p><p>│   │   └── router.js            # Enruta por canal (wa/ig/fb)</p><p>│   │</p><p>│   ├── channels/</p><p>│   │   ├── whatsapp/</p><p>│   │   │   ├── handler.js       # Parsea eventos WA</p><p>│   │   │   └── sender.js        # Envía mensajes via WA API</p><p>│   │   ├── messenger/</p><p>│   │   │   ├── handler.js       # Parsea eventos Messenger</p><p>│   │   │   └── sender.js        # Envía via Messenger API</p><p>│   │   └── instagram/</p><p>│   │       ├── handler.js       # Parsea eventos Instagram</p><p>│   │       └── sender.js        # Envía via Instagram API</p><p>│   │</p><p>│   ├── core/</p><p>│   │   ├── messageProcessor.js  # Orquestador principal</p><p>│   │   ├── intentDetector.js    # Clasifica intención con Claude</p><p>│   │   ├── responseGenerator.js # Genera respuesta con Claude</p><p>│   │   └── humanEscalation.js   # Lógica de escalamiento</p><p>│   │</p><p>│   ├── services/</p><p>│   │   ├── claudeService.js     # Wrapper Anthropic SDK</p><p>│   │   ├── configService.js     # Lee/cachea config del bot</p><p>│   │   └── conversationService.js # Manejo de historial</p><p>│   │</p><p>│   ├── models/                  # Queries SQL (pg)</p><p>│   │   ├── User.js</p><p>│   │   ├── Conversation.js</p><p>│   │   ├── Message.js</p><p>│   │   ├── BotConfig.js</p><p>│   │   └── FAQ.js</p><p>│   │</p><p>│   ├── admin/</p><p>│   │   ├── routes.js            # Rutas del panel admin</p><p>│   │   ├── controllers/</p><p>│   │   └── middleware/auth.js   # JWT middleware</p><p>│   │</p><p>│   └── utils/</p><p>│       ├── logger.js            # Winston logger</p><p>│       └── helpers.js</p><p>│</p><p>├── migrations/                  # SQL migrations</p><p>│   ├── 001_create_users.sql</p><p>│   ├── 002_create_conversations.sql</p><p>│   ├── 003_create_messages.sql</p><p>│   ├── 004_create_bot_config.sql</p><p>│   └── 005_create_faqs.sql</p><p>│</p><p>├── .env.example</p><p>├── package.json</p><p>├── Dockerfile</p><p>└── index.js                     # Entry point</p><h1><strong>3. Diseño de Base de Datos</strong></h1><h2><strong>3.1 Schema PostgreSQL</strong></h2><p><strong>  migrations/001_create_users.sql</strong></p><p> </p><p>-- USUARIOS (contactos que escriben al bot)</p><p>CREATE TABLE users (</p><p>  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),</p><p>  platform      VARCHAR(20) NOT NULL,   -- 'whatsapp' | 'messenger' | 'instagram'</p><p>  platform_id   VARCHAR(100) NOT NULL,  -- ID del usuario en la plataforma</p><p>  name          VARCHAR(255),</p><p>  phone         VARCHAR(50),</p><p>  profile_pic   TEXT,</p><p>  created_at    TIMESTAMPTZ DEFAULT NOW(),</p><p>  updated_at    TIMESTAMPTZ DEFAULT NOW(),</p><p>  UNIQUE(platform, platform_id)</p><p>);</p><p> </p><p>-- CONVERSACIONES</p><p>CREATE TABLE conversations (</p><p>  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),</p><p>  user_id       UUID REFERENCES users(id) ON DELETE CASCADE,</p><p>  platform      VARCHAR(20) NOT NULL,</p><p>  status        VARCHAR(20) DEFAULT 'active',  -- 'active' | 'escalated' | 'closed'</p><p>  agent_id      UUID,                           -- agente humano asignado (si escaló)</p><p>  created_at    TIMESTAMPTZ DEFAULT NOW(),</p><p>  updated_at    TIMESTAMPTZ DEFAULT NOW()</p><p>);</p><p> </p><p>-- MENSAJES</p><p>CREATE TABLE messages (</p><p>  id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),</p><p>  conversation_id UUID REFERENCES conversations(id) ON DELETE CASCADE,</p><p>  role           VARCHAR(10) NOT NULL,   -- 'user' | 'assistant'</p><p>  content        TEXT NOT NULL,</p><p>  intent         VARCHAR(50),            -- 'general' | 'quote' | 'availability' | etc.</p><p>  platform_msg_id VARCHAR(200),          -- ID del mensaje en la plataforma</p><p>  created_at     TIMESTAMPTZ DEFAULT NOW()</p><p>);</p><p> </p><p>-- CONFIGURACIÓN DEL BOT (una fila por agencia)</p><p>CREATE TABLE bot_config (</p><p>  id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),</p><p>  agency_name    VARCHAR(255) NOT NULL,</p><p>  welcome_msg    TEXT,</p><p>  out_of_hours_msg TEXT,</p><p>  business_hours JSONB,                  -- {"mon":{"open":"09:00","close":"18:00"}...}</p><p>  contact_info   JSONB,                  -- {"phone":"...","email":"...","address":"..."}</p><p>  bot_personality TEXT,                  -- prompt de personalidad</p><p>  escalation_keywords TEXT[],            -- palabras que disparan escalamiento</p><p>  updated_at     TIMESTAMPTZ DEFAULT NOW()</p><p>);</p><p> </p><p>-- FAQs</p><p>CREATE TABLE faqs (</p><p>  id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),</p><p>  question       TEXT NOT NULL,</p><p>  answer         TEXT NOT NULL,</p><p>  category       VARCHAR(100),</p><p>  is_active      BOOLEAN DEFAULT TRUE,</p><p>  created_at     TIMESTAMPTZ DEFAULT NOW()</p><p>);</p><p> </p><p>-- LOGS DE EVENTOS</p><p>CREATE TABLE event_logs (</p><p>  id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),</p><p>  event_type     VARCHAR(50),           -- 'webhook_received' | 'ai_response' | 'escalation' | etc.</p><p>  platform       VARCHAR(20),</p><p>  payload        JSONB,</p><p>  error          TEXT,</p><p>  created_at     TIMESTAMPTZ DEFAULT NOW()</p><p>);</p><p> </p><p>-- ÍNDICES</p><p>CREATE INDEX idx_messages_conversation ON messages(conversation_id);</p><p>CREATE INDEX idx_conversations_user ON conversations(user_id);</p><p>CREATE INDEX idx_users_platform ON users(platform, platform_id);</p><h1><strong>4. Código Base Node.js</strong></h1><h2><strong>4.1 Entry Point (index.js)</strong></h2><p><strong>  index.js</strong></p><p> </p><p>require('dotenv').config();</p><p>const express = require('express');</p><p>const webhookRoutes = require('./src/webhooks');</p><p>const adminRoutes = require('./src/admin/routes');</p><p>const { connectDB } = require('./src/config/database');</p><p>const logger = require('./src/utils/logger');</p><p> </p><p>const app = express();</p><p>app.use(express.json());</p><p> </p><p>app.use('/webhook', webhookRoutes);</p><p>app.use('/admin', adminRoutes);</p><p>app.get('/health', (req, res) =&gt; res.json({ status: 'ok' }));</p><p> </p><p>const PORT = process.env.PORT || 3000;</p><p> </p><p>(async () =&gt; {</p><p>  await connectDB();</p><p>  app.listen(PORT, () =&gt; logger.info(`Vicka Bot running on port ${PORT}`));</p><p>})();</p><h2><strong>4.2 Webhook Router con Validación Meta</strong></h2><p><strong>  src/webhooks/index.js</strong></p><p> </p><p>const express = require('express');</p><p>const router = express.Router();</p><p>const { validateMetaSignature } = require('./validator');</p><p>const { routeWebhook } = require('./router');</p><p> </p><p>// ── Verificación del webhook (setup inicial con Meta)</p><p>router.get('/', (req, res) =&gt; {</p><p>  const mode      = req.query['hub.mode'];</p><p>  const token     = req.query['hub.verify_token'];</p><p>  const challenge = req.query['hub.challenge'];</p><p> </p><p>  if (mode === 'subscribe' &amp;&amp; token === process.env.WEBHOOK_VERIFY_TOKEN) {</p><p>    return res.status(200).send(challenge);</p><p>  }</p><p>  return res.sendStatus(403);</p><p>});</p><p> </p><p>// ── Recepción de eventos</p><p>router.post('/', validateMetaSignature, async (req, res) =&gt; {</p><p>  // Responder 200 inmediatamente (Meta requiere respuesta en &lt; 5s)</p><p>  res.sendStatus(200);</p><p>  // Procesar de forma asíncrona</p><p>  try {</p><p>    await routeWebhook(req.body);</p><p>  } catch (err) {</p><p>    require('../utils/logger').error('Webhook processing error', { err });</p><p>  }</p><p>});</p><p> </p><p>module.exports = router;</p><p><strong>  src/webhooks/validator.js</strong></p><p> </p><p>const crypto = require('crypto');</p><p> </p><p>function validateMetaSignature(req, res, next) {</p><p>  const signature = req.headers['x-hub-signature-256'];</p><p>  if (!signature) return res.sendStatus(401);</p><p> </p><p>  const expected = 'sha256=' + crypto</p><p>    .createHmac('sha256', process.env.META_APP_SECRET)</p><p>    .update(JSON.stringify(req.body))</p><p>    .digest('hex');</p><p> </p><p>  if (!crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected))) {</p><p>    return res.sendStatus(401);</p><p>  }</p><p>  next();</p><p>}</p><p><strong>  src/webhooks/router.js</strong></p><p> </p><p>const whatsappHandler = require('../channels/whatsapp/handler');</p><p>const messengerHandler = require('../channels/messenger/handler');</p><p>const instagramHandler = require('../channels/instagram/handler');</p><p>const logger = require('../utils/logger');</p><p> </p><p>async function routeWebhook(body) {</p><p>  const { object, entry } = body;</p><p> </p><p>  // WhatsApp usa object: 'whatsapp_business_account'</p><p>  if (object === 'whatsapp_business_account') {</p><p>    return whatsappHandler.handle(entry);</p><p>  }</p><p> </p><p>  // Instagram y Messenger usan object: 'instagram' | 'page'</p><p>  if (object === 'instagram') {</p><p>    return instagramHandler.handle(entry);</p><p>  }</p><p> </p><p>  if (object === 'page') {</p><p>    return messengerHandler.handle(entry);</p><p>  }</p><p> </p><p>  logger.warn('Unknown webhook object', { object });</p><p>}</p><p> </p><p>module.exports = { routeWebhook };</p><h2><strong>4.3 WhatsApp Handler y Sender</strong></h2><p><strong>  src/channels/whatsapp/handler.js</strong></p><p> </p><p>const { processMessage } = require('../../core/messageProcessor');</p><p>const logger = require('../../utils/logger');</p><p> </p><p>async function handle(entries) {</p><p>  for (const entry of entries) {</p><p>    for (const change of entry.changes ?? []) {</p><p>      const value = change.value;</p><p>      if (!value?.messages) continue;</p><p> </p><p>      for (const message of value.messages) {</p><p>        if (message.type !== 'text') continue; // Por ahora solo texto</p><p> </p><p>        const normalized = {</p><p>          platform:       'whatsapp',</p><p>          platform_user_id: message.from,</p><p>          platform_msg_id:  message.id,</p><p>          text:           message.text.body,</p><p>          name:           value.contacts?.[0]?.profile?.name ?? 'Usuario'</p><p>        };</p><p> </p><p>        await processMessage(normalized);</p><p>      }</p><p>    }</p><p>  }</p><p>}</p><p> </p><p>module.exports = { handle };</p><p><strong>  src/channels/whatsapp/sender.js</strong></p><p> </p><p>const axios = require('axios');</p><p> </p><p>const WA_URL = `https://graph.facebook.com/v19.0/${process.env.WA_PHONE_NUMBER_ID}/messages`;</p><p> </p><p>async function sendMessage(to, text) {</p><p>  await axios.post(WA_URL, {</p><p>    messaging_product: 'whatsapp',</p><p>    recipient_type:    'individual',</p><p>    to,</p><p>    type: 'text',</p><p>    text: { preview_url: false, body: text }</p><p>  }, {</p><p>    headers: { Authorization: `Bearer ${process.env.META_ACCESS_TOKEN}` }</p><p>  });</p><p>}</p><p> </p><p>module.exports = { sendMessage };</p><h2><strong>4.4 Messenger Sender</strong></h2><p><strong>  src/channels/messenger/sender.js</strong></p><p> </p><p>const axios = require('axios');</p><p> </p><p>const GRAPH_URL = 'https://graph.facebook.com/v19.0/me/messages';</p><p> </p><p>async function sendMessage(recipientId, text) {</p><p>  await axios.post(GRAPH_URL, {</p><p>    recipient: { id: recipientId },</p><p>    message:   { text },</p><p>    messaging_type: 'RESPONSE'</p><p>  }, {</p><p>    params:  { access_token: process.env.FB_PAGE_ACCESS_TOKEN },</p><p>    headers: { 'Content-Type': 'application/json' }</p><p>  });</p><p>}</p><p> </p><p>module.exports = { sendMessage };</p><h2><strong>4.5 Instagram Sender</strong></h2><p><strong>  src/channels/instagram/sender.js</strong></p><p> </p><p>const axios = require('axios');</p><p> </p><p>const GRAPH_URL = 'https://graph.facebook.com/v19.0/me/messages';</p><p> </p><p>async function sendMessage(recipientId, text) {</p><p>  await axios.post(GRAPH_URL, {</p><p>    recipient: { id: recipientId },</p><p>    message:   { text }</p><p>  }, {</p><p>    params:  { access_token: process.env.IG_ACCESS_TOKEN },</p><p>    headers: { 'Content-Type': 'application/json' }</p><p>  });</p><p>}</p><p> </p><p>module.exports = { sendMessage };</p><h1><strong>5. Procesamiento de Mensajes con IA</strong></h1><h2><strong>5.1 Orquestador Principal</strong></h2><p><strong>  src/core/messageProcessor.js</strong></p><p> </p><p>const { getOrCreateUser }      = require('../models/User');</p><p>const { getOrCreateConversation } = require('../models/Conversation');</p><p>const { saveMessage }           = require('../models/Message');</p><p>const { detectIntent }          = require('./intentDetector');</p><p>const { generateResponse }      = require('./responseGenerator');</p><p>const { checkEscalation }       = require('./humanEscalation');</p><p>const { getSender }             = require('../utils/helpers');</p><p>const configService             = require('../services/configService');</p><p>const logger                    = require('../utils/logger');</p><p> </p><p>async function processMessage(normalized) {</p><p>  const { platform, platform_user_id, text, name, platform_msg_id } = normalized;</p><p> </p><p>  // 1. Obtener o crear usuario y conversación</p><p>  const user = await getOrCreateUser({ platform, platform_user_id, name });</p><p>  const conversation = await getOrCreateConversation({ userId: user.id, platform });</p><p> </p><p>  if (conversation.status === 'escalated') {</p><p>    logger.info('Conversation escalated, skipping bot', { conversationId: conversation.id });</p><p>    return;</p><p>  }</p><p> </p><p>  // 2. Guardar mensaje entrante</p><p>  await saveMessage({ conversationId: conversation.id, role: 'user', content: text, platform_msg_id });</p><p> </p><p>  // 3. Verificar horario de atención</p><p>  const config = await configService.getConfig();</p><p>  if (!configService.isBusinessHours(config)) {</p><p>    const sender = getSender(platform);</p><p>    await sender.sendMessage(platform_user_id, config.out_of_hours_msg);</p><p>    return;</p><p>  }</p><p> </p><p>  // 4. Detectar si necesita escalamiento</p><p>  const needsEscalation = await checkEscalation(text, config.escalation_keywords);</p><p>  if (needsEscalation) {</p><p>    await handleEscalation(conversation.id, platform, platform_user_id, config);</p><p>    return;</p><p>  }</p><p> </p><p>  // 5. Detectar intención</p><p>  const intent = await detectIntent(text);</p><p> </p><p>  // 6. Generar respuesta con Claude</p><p>  const response = await generateResponse({ conversationId: conversation.id, userMessage: text, intent, config });</p><p> </p><p>  // 7. Guardar y enviar respuesta</p><p>  await saveMessage({ conversationId: conversation.id, role: 'assistant', content: response, intent });</p><p>  const sender = getSender(platform);</p><p>  await sender.sendMessage(platform_user_id, response);</p><p>}</p><p> </p><p>async function handleEscalation(conversationId, platform, userId, config) {</p><p>  const { updateConversationStatus } = require('../models/Conversation');</p><p>  await updateConversationStatus(conversationId, 'escalated');</p><p>  const sender = getSender(platform);</p><p>  await sender.sendMessage(userId, 'Un momento, te estoy conectando con uno de nuestros agentes. 😊 En breve alguien de nuestro equipo te va a atender.');</p><p>}</p><p> </p><p>module.exports = { processMessage };</p><h2><strong>5.2 Servicio Claude (IA)</strong></h2><p><strong>  src/services/claudeService.js</strong></p><p> </p><p>const Anthropic = require('@anthropic-ai/sdk');</p><p>const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });</p><p> </p><p>/**</p><p> * Llama a Claude con un system prompt y un historial de mensajes.</p><p> */</p><p>async function callClaude({ systemPrompt, messages, maxTokens = 600 }) {</p><p>  const response = await client.messages.create({</p><p>    model:      'claude-sonnet-4-20250514',</p><p>    max_tokens: maxTokens,</p><p>    system:     systemPrompt,</p><p>    messages    // [{ role: 'user'|'assistant', content: '...' }]</p><p>  });</p><p> </p><p>  return response.content[0].text;</p><p>}</p><p> </p><p>module.exports = { callClaude };</p><h2><strong>5.3 Generador de Respuestas con Contexto</strong></h2><p><strong>  src/core/responseGenerator.js</strong></p><p> </p><p>const { callClaude }          = require('../services/claudeService');</p><p>const { getHistory }          = require('../models/Message');</p><p>const { getFAQsFormatted }    = require('../models/FAQ');</p><p> </p><p>async function generateResponse({ conversationId, userMessage, intent, config }) {</p><p>  // Obtener historial reciente (últimos 10 mensajes para no exceder contexto)</p><p>  const history = await getHistory(conversationId, 10);</p><p> </p><p>  // Obtener FAQs activas como contexto</p><p>  const faqs = await getFAQsFormatted();</p><p> </p><p>  const systemPrompt = buildSystemPrompt(config, faqs);</p><p> </p><p>  // Formatear historial para Claude</p><p>  const messages = [</p><p>    ...history.map(m =&gt; ({ role: m.role, content: m.content })),</p><p>    { role: 'user', content: userMessage }</p><p>  ];</p><p> </p><p>  const response = await callClaude({ systemPrompt, messages });</p><p>  return response;</p><p>}</p><p> </p><p>function buildSystemPrompt(config, faqs) {</p><p>  return `</p><p>Sos el asistente virtual de ${config.agency_name}, una agencia de viajes especializada en turismo.</p><p> </p><p>PERSONALIDAD:</p><p>${config.bot_personality ?? 'Amigable, profesional y orientado a ventas. Siempre empáticos y entusiastas con los destinos.'}</p><p> </p><p>TU OBJETIVO:</p><p>- Ayudar a los clientes a encontrar el paquete turístico ideal</p><p>- Responder preguntas sobre destinos, precios y disponibilidad</p><p>- Capturar leads interesados para que un agente los contacte</p><p>- Derivar al agente humano cuando corresponda</p><p> </p><p>INFORMACIÓN DE CONTACTO:</p><p>${JSON.stringify(config.contact_info ?? {})}</p><p> </p><p>PREGUNTAS FRECUENTES Y PRODUCTOS:</p><p>${faqs}</p><p> </p><p>REGLAS ESTRICTAS:</p><p>- Si no sabés el precio exacto, decí que vas a consultar y ofrecé contacto con el agente</p><p>- No inventes precios ni disponibilidades</p><p>- Siempre terminá con una llamada a la acción (¿querés que un agente te contacte? etc.)</p><p>- Respondé siempre en español</p><p>- Máximo 3 párrafos cortos por respuesta</p><p>- Usá emojis con moderación para ser más amigable</p><p> </p><p>INTENCIÓN DETECTADA: ${intent}</p><p>`.trim();</p><p>}</p><p> </p><p>module.exports = { generateResponse };</p><h2><strong>5.4 Detector de Intención</strong></h2><p><strong>  src/core/intentDetector.js</strong></p><p> </p><p>const { callClaude } = require('../services/claudeService');</p><p> </p><p>const INTENTS = ['general', 'quote', 'availability', 'complaint', 'human_request'];</p><p> </p><p>async function detectIntent(text) {</p><p>  const response = await callClaude({</p><p>    systemPrompt: `</p><p>Clasificá el siguiente mensaje de un cliente de una agencia de viajes en UNA de estas categorías:</p><p>- general: consulta general sobre destinos, info, precios aproximados</p><p>- quote: solicita presupuesto concreto o cotización</p><p>- availability: consulta disponibilidad para fechas específicas</p><p>- complaint: reclamo, queja o insatisfacción</p><p>- human_request: quiere hablar con una persona</p><p> </p><p>Respondé SOLO con la categoría, sin explicación.`,</p><p>    messages: [{ role: 'user', content: text }],</p><p>    maxTokens: 10</p><p>  });</p><p> </p><p>  const detected = response.trim().toLowerCase();</p><p>  return INTENTS.includes(detected) ? detected : 'general';</p><p>}</p><p> </p><p>module.exports = { detectIntent };</p><h1><strong>6. Sistema de Configuración del Bot</strong></h1><p><strong>  src/services/configService.js</strong></p><p> </p><p>const { getConfig: getConfigFromDB } = require('../models/BotConfig');</p><p>const redis = require('../config/redis');</p><p>const logger = require('../utils/logger');</p><p> </p><p>const CACHE_KEY = 'bot_config';</p><p>const CACHE_TTL = 300; // 5 minutos</p><p> </p><p>async function getConfig() {</p><p>  // Intentar desde Redis</p><p>  const cached = await redis.get(CACHE_KEY);</p><p>  if (cached) return JSON.parse(cached);</p><p> </p><p>  // Si no, desde DB</p><p>  const config = await getConfigFromDB();</p><p>  await redis.setEx(CACHE_KEY, CACHE_TTL, JSON.stringify(config));</p><p>  return config;</p><p>}</p><p> </p><p>function isBusinessHours(config) {</p><p>  const now = new Date();</p><p>  const day = ['sun','mon','tue','wed','thu','fri','sat'][now.getDay()];</p><p>  const hours = config.business_hours?.[day];</p><p>  if (!hours) return false;</p><p> </p><p>  const [openH, openM]   = hours.open.split(':').map(Number);</p><p>  const [closeH, closeM] = hours.close.split(':').map(Number);</p><p>  const currentMins = now.getHours() * 60 + now.getMinutes();</p><p>  const openMins    = openH * 60 + openM;</p><p>  const closeMins   = closeH * 60 + closeM;</p><p> </p><p>  return currentMins &gt;= openMins &amp;&amp; currentMins &lt;= closeMins;</p><p>}</p><p> </p><p>async function invalidateCache() {</p><p>  await redis.del(CACHE_KEY);</p><p>}</p><p> </p><p>module.exports = { getConfig, isBusinessHours, invalidateCache };</p><h2><strong>6.1 Variables de entorno requeridas</strong></h2><p><strong>  .env.example</strong></p><p> </p><p># Meta APIs</p><p>META_APP_SECRET=tu_app_secret_de_meta</p><p>WEBHOOK_VERIFY_TOKEN=token_que_vos_elegis</p><p> </p><p># WhatsApp</p><p>WA_PHONE_NUMBER_ID=1234567890</p><p>META_ACCESS_TOKEN=tu_token_permanente_wa</p><p> </p><p># Messenger</p><p>FB_PAGE_ACCESS_TOKEN=tu_page_access_token</p><p> </p><p># Instagram</p><p>IG_ACCESS_TOKEN=tu_ig_token</p><p> </p><p># Anthropic</p><p>ANTHROPIC_API_KEY=sk-ant-...</p><p> </p><p># Base de datos</p><p>DATABASE_URL=postgresql://user:pass@host:5432/vicka_bot</p><p>REDIS_URL=redis://localhost:6379</p><p> </p><p># Admin panel</p><p>JWT_SECRET=un_secreto_muy_largo_y_seguro</p><p>ADMIN_EMAIL=admin@vickaviajes.com</p><p>ADMIN_PASSWORD=password_inicial</p><p> </p><p># Entorno</p><p>NODE_ENV=production</p><p>PORT=3000</p><h1><strong>7. Prompts para Desarrollo con Antigravity</strong></h1><p>Los siguientes prompts están optimizados para ser ejecutados en Antigravity de forma secuencial. Cada uno construye sobre el anterior.</p><p><em>⚡ Ejecutar en orden. Cada prompt asume que el anterior fue completado. Copiar y pegar cada bloque completo en Antigravity.</em></p><p><strong>  PROMPT 01 — Scaffold del proyecto</strong></p><p><em>Creá un proyecto Node.js llamado "vicka-turismo-bot" listo para producción. El proyecto debe usar Express 4, tener una estructura de carpetas modular con separación por capas: webhooks/, channels/ (whatsapp/messenger/instagram), core/, services/, models/, admin/, utils/. Incluí package.json con las dependencias: express, @anthropic-ai/sdk, axios, pg, ioredis, jsonwebtoken, bcrypt, dotenv, winston. Agregá scripts de npm: start, dev (nodemon), migrate. Creá un .env.example con placeholders para META_APP_SECRET, WEBHOOK_VERIFY_TOKEN, WA_PHONE_NUMBER_ID, META_ACCESS_TOKEN, FB_PAGE_ACCESS_TOKEN, IG_ACCESS_TOKEN, ANTHROPIC_API_KEY, DATABASE_URL, REDIS_URL, JWT_SECRET. Creá el index.js entry point que inicialice Express, conecte a la DB, monte las rutas /webhook y /admin, y exponga /health.</em></p><p><strong>  PROMPT 02 — Webhook con validación Meta HMAC</strong></p><p><em>En el archivo src/webhooks/index.js creá un router Express con dos rutas: GET /webhook para verificar el token con Meta (hub.mode, hub.verify_token, hub.challenge) y POST /webhook para recibir eventos. El POST debe primero validar la firma HMAC-SHA256 del header x-hub-signature-256 usando el META_APP_SECRET del .env, usando crypto.timingSafeEqual para evitar timing attacks. Si la firma no es válida, retornar 401. Si es válida, responder 200 INMEDIATAMENTE y luego procesar el evento de forma asíncrona (fire and forget con try/catch). El procesamiento asíncrono debe llamar a un routeWebhook(body) que identifique el tipo de objeto ('whatsapp_business_account', 'instagram', 'page') y lo derive al handler correspondiente.</em></p><p><strong>  PROMPT 03 — Handlers por canal (WhatsApp, Messenger, Instagram)</strong></p><p><em>Creá handlers individuales para cada canal. Para src/channels/whatsapp/handler.js: parsear el payload de Meta WhatsApp Business, extraer entry &gt; changes &gt; value &gt; messages, filtrar solo messages de tipo 'text', normalizar a un objeto {platform: 'whatsapp', platform_user_id, platform_msg_id, text, name}. Para src/channels/messenger/handler.js: parsear entry &gt; messaging, extraer sender.id y message.text. Para src/channels/instagram/handler.js: igual que messenger pero usando el formato de Instagram. Cada handler debe llamar a processMessage(normalized) del core. Creá también los senders correspondientes: whatsapp/sender.js usando POST a graph.facebook.com/v19.0/{WA_PHONE_NUMBER_ID}/messages con el formato de texto de WhatsApp. messenger/sender.js y instagram/sender.js usando POST a /me/messages con el access_token como query param.</em></p><p><strong>  PROMPT 04 — Base de datos PostgreSQL con pg</strong></p><p><em>Creá src/config/database.js que inicialice un Pool de pg usando DATABASE_URL del .env con maxConnections: 20 y un retry automático si falla la conexión. Creá los archivos de migration SQL en migrations/ para estas tablas: users (id UUID, platform VARCHAR, platform_id VARCHAR, name, phone, created_at, UNIQUE platform+platform_id), conversations (id UUID, user_id FK, platform, status VARCHAR default 'active', agent_id UUID, timestamps), messages (id UUID, conversation_id FK, role VARCHAR, content TEXT, intent VARCHAR, platform_msg_id, created_at), bot_config (id UUID, agency_name, welcome_msg, out_of_hours_msg, business_hours JSONB, contact_info JSONB, bot_personality TEXT, escalation_keywords TEXT[]), faqs (id UUID, question TEXT, answer TEXT, category, is_active BOOLEAN). Creá los models en src/models/ con funciones async: getOrCreateUser, getOrCreateConversation, saveMessage, getHistory, getConfig, getFAQsFormatted.</em></p><p><strong>  PROMPT 05 — Servicio Claude con contexto y FAQs</strong></p><p><em>Creá src/services/claudeService.js que importe @anthropic-ai/sdk, instancie el client con la API key, y exporte una función callClaude({systemPrompt, messages, maxTokens=600}) que llame a client.messages.create con model 'claude-sonnet-4-20250514'. Creá src/core/responseGenerator.js que: (1) obtenga el historial de los últimos 10 mensajes de la conversación, (2) obtenga las FAQs activas formateadas como string, (3) construya un systemPrompt dinámico usando la configuración del bot (agency_name, contact_info, bot_personality, FAQs), (4) llame a callClaude con el historial más el mensaje actual del usuario. El system prompt debe instruir al bot a: responder en español, máximo 3 párrafos, no inventar precios, siempre terminar con CTA, usar el intento detectado para contextualizar la respuesta.</em></p><p><strong>  PROMPT 06 — Detector de intención y escalamiento</strong></p><p><em>Creá src/core/intentDetector.js que use una llamada liviana a Claude (maxTokens: 10) para clasificar el mensaje del usuario en: 'general', 'quote', 'availability', 'complaint', 'human_request'. El system prompt debe listar las categorías y pedir responder SOLO con la categoría. Creá src/core/humanEscalation.js con una función checkEscalation(text, keywords) que: (1) verifique si el texto contiene alguna keyword de escalamiento de la config (ej: "hablar con alguien", "agente", "persona"), (2) también retorne true si detectIntent devuelve 'human_request'. Creá src/core/messageProcessor.js como orquestador que: verifique si la conversación ya está escalada (skip bot), compruebe horario de atención (enviar mensaje fuera de horario si corresponde), detecte escalamiento, detecte intención, genere respuesta con Claude, guarde todo en DB y envíe la respuesta al canal correcto usando getSender(platform).</em></p><p><strong>  PROMPT 07 — Config Service con Redis caché</strong></p><p><em>Creá src/config/redis.js que conecte a Redis usando ioredis con la REDIS_URL del .env. Creá src/services/configService.js que: exporte getConfig() que primero intente leer de Redis con clave 'bot_config' (TTL 5 minutos), si no existe lea de PostgreSQL y guarde en Redis. Exporte isBusinessHours(config) que reciba la config con business_hours JSONB (formato {mon: {open: "09:00", close: "18:00"}, ...}) y compare con la hora actual en Argentina (UTC-3). Exporte invalidateCache() para cuando se edite la config desde el panel admin. Creá src/utils/helpers.js con getSender(platform) que retorne el sender correcto según el canal.</em></p><p><strong>  PROMPT 08 — Panel admin con JWT</strong></p><p><em>Creá src/admin/routes.js con endpoints REST protegidos por JWT: POST /admin/login (recibe email+password, valida contra ADMIN_EMAIL/ADMIN_PASSWORD del .env hasheado con bcrypt, retorna JWT con 24h de expiración). GET /admin/config (retorna la config actual del bot). PUT /admin/config (actualiza campos de bot_config e invalida el Redis caché). GET /admin/faqs (lista FAQs activas). POST /admin/faqs (crea nueva FAQ). PUT /admin/faqs/:id (edita FAQ). DELETE /admin/faqs/:id (desactiva FAQ, soft delete). GET /admin/conversations (lista conversaciones con paginación, 20 por página). GET /admin/conversations/:id/messages (historial de un chat). POST /admin/conversations/:id/escalate (marca como escalada manualmente). GET /admin/stats (retorna totales: mensajes hoy, conversaciones activas, escalaciones). Cada endpoint debe tener try/catch y retornar errores con formato {error: "mensaje"}.</em></p><p><strong>  PROMPT 09 — Dockerfile y configuración de deploy en Railway</strong></p><p><em>Creá un Dockerfile multi-stage optimizado para Node.js 20 Alpine: stage builder que instale solo dependencias de producción, stage final que copie el resultado. Exponé el PORT 3000. Creá un railway.json con la configuración de deploy: build command 'npm ci --only=production', start command 'node index.js', healthcheck en /health cada 30s. Creá un script migrations/run.js que ejecute todos los archivos .sql de la carpeta migrations en orden usando pg, para correr con 'npm run migrate'. Creá un src/utils/logger.js usando winston con formato JSON en producción y formato colorido en desarrollo, incluyendo timestamp y nivel. Actualizá el package.json con todos los scripts necesarios.</em></p><p><strong>  PROMPT 10 — Conversaciones de ejemplo y testing</strong></p><p><em>Agregá en src/utils/testConversations.js tres conversaciones de ejemplo completas para testear el sistema. Conversación 1 (consulta de paquete): usuario pregunta por Río de Janeiro en febrero, el bot responde con info del paquete, el usuario pregunta precio, el bot da precio aproximado y ofrece conectar con agente. Conversación 2 (escalamiento): usuario pregunta por Cancún, luego dice "quiero hablar con una persona", el bot confirma el escalamiento. Conversación 3 (fuera de horario): usuario escribe a las 23hs, bot responde con mensaje de fuera de horario con horarios de atención. Creá también un script de seed para popular la base de datos con: una configuración de bot para "Vicka Turismo", horarios lun-vie 9-18 y sab 9-13, 5 FAQs de ejemplo sobre medios de pago, política de cancelación, documentación requerida, seguro de viaje y grupos.</em></p><h1><strong>8. Flujo Completo de un Mensaje</strong></h1><table><tr><td><p><strong>#</strong></p></td><td><p><strong>Paso</strong></p></td><td><p><strong>Detalle</strong></p></td></tr><tr><td><p><strong>1</strong></p></td><td><p><strong>Meta envía webhook POST /webhook</strong></p></td><td><p>Payload JSON con el mensaje del usuario en el formato de la plataforma</p></td></tr><tr><td><p><strong>2</strong></p></td><td><p><strong>Validar firma HMAC-SHA256</strong></p></td><td><p>El middleware validator.js verifica x-hub-signature-256. 401 si falla.</p></td></tr><tr><td><p><strong>3</strong></p></td><td><p><strong>Responder 200 OK inmediato</strong></p></td><td><p>Meta requiere respuesta en &lt; 5 seg. Se responde antes de procesar.</p></td></tr><tr><td><p><strong>4</strong></p></td><td><p><strong>Identificar canal</strong></p></td><td><p>router.js analiza body.object para determinar whatsapp/instagram/page</p></td></tr><tr><td><p><strong>5</strong></p></td><td><p><strong>Normalizar mensaje</strong></p></td><td><p>El handler del canal extrae el texto, ID del usuario, nombre, ID del mensaje.</p></td></tr><tr><td><p><strong>6</strong></p></td><td><p><strong>Upsert usuario y conversación</strong></p></td><td><p>Se busca o crea el usuario y la conversación activa en PostgreSQL.</p></td></tr><tr><td><p><strong>7</strong></p></td><td><p><strong>Guardar mensaje entrante</strong></p></td><td><p>Se persiste el mensaje con role='user' en la tabla messages.</p></td></tr><tr><td><p><strong>8</strong></p></td><td><p><strong>Verificar horario</strong></p></td><td><p>configService.isBusinessHours() — si es fuera de horario, envía msg automático.</p></td></tr><tr><td><p><strong>9</strong></p></td><td><p><strong>Verificar escalamiento</strong></p></td><td><p>Si hay keywords o el usuario pidió hablar con alguien → handoff a humano.</p></td></tr><tr><td><p><strong>10</strong></p></td><td><p><strong>Detectar intención</strong></p></td><td><p>Llamada liviana a Claude para clasificar en general/quote/availability/complaint.</p></td></tr><tr><td><p><strong>11</strong></p></td><td><p><strong>Generar respuesta</strong></p></td><td><p>Claude recibe: system prompt con config+FAQs + historial + mensaje actual.</p></td></tr><tr><td><p><strong>12</strong></p></td><td><p><strong>Guardar respuesta</strong></p></td><td><p>Se persiste la respuesta con role='assistant' e intent en la tabla messages.</p></td></tr><tr><td><p><strong>13</strong></p></td><td><p><strong>Enviar al canal</strong></p></td><td><p>El sender correspondiente llama a la Graph API de Meta con la respuesta.</p></td></tr></table><h1><strong>9. Recomendaciones para Escalar</strong></h1><h2><strong>Fase 1 — MVP (hasta 500 mensajes/día)</strong></h2><ul><li>Railway con un único servicio Node.js + PostgreSQL addon</li><li>Redis addon de Railway para caché</li><li>Una sola instancia del bot</li></ul><h2><strong>Fase 2 — Crecimiento (hasta 5.000 mensajes/día)</strong></h2><ul><li>Separar el procesamiento en una cola con BullMQ + Redis</li><li>Worker separado para llamadas a Claude (evita bloqueos)</li><li>Múltiples instancias Node.js detrás de load balancer</li><li>Read replica de PostgreSQL para queries del panel admin</li></ul><h2><strong>Fase 3 — Escala alta (10.000+ mensajes/día)</strong></h2><ul><li>Migrar a arquitectura de microservicios completa</li><li>Kafka o RabbitMQ para mensajería entre servicios</li><li>CDN para assets del panel admin</li><li>Monitoreo con Datadog o Grafana + Prometheus</li><li>Rate limiting por usuario/plataforma</li></ul><h2><strong>Buenas prácticas inmediatas</strong></h2><ul><li>Siempre responder 200 a Meta antes de procesar (evitar retries)</li><li>Idempotencia: usar platform_msg_id para no procesar el mismo mensaje dos veces</li><li>Limitar historial a 10 mensajes para controlar costos de tokens de Claude</li><li>Logs estructurados en JSON desde el día 1 (Winston)</li><li>Variables de entorno nunca en código, siempre en .env / Railway vars</li></ul><p><strong>  Chat Bot Vicka Turismo — Sistema listo para producción  </strong></p><p><em>  Desarrollado como base para Antigravity  </em></p>